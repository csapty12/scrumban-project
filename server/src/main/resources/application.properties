#spring.datasource.url = jdbc:mysql://localhost:3306/scrumban?useSSL=false
#spring.datasource.username = root
#spring.datasource.password = password
#
#
### Hibernate Properties
## The SQL dialect makes Hibernate generate better SQL for the chosen database
#spring.jpa.properties.hibernate.dialect = org.hibernate.dialect.MySQL5InnoDBDialect
#
## Hibernate ddl auto (create, create-drop, validate, update)
#spring.jpa.hibernate.ddl-auto = update
#spring.datasource.url = jdbc:mysql://127.0.0.1:2012/spring_app_db?useSSL=false
spring.datasource.url=jdbc:mysql://127.0.0.1:3306/scrumban?useSSL=false
spring.datasource.username=root
spring.datasource.password=password
## Hibernate Properties
# The SQL dialect makes Hibernate generate better SQL for the chosen database
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL5InnoDBDialect
# Hibernate ddl auto (create, create-drop, validate, update)
spring.jpa.hibernate.ddl-auto=update
aunacthenticated.signup.url.access=/api/users/**
jwt.secret=SecretKeyToGenJwts
jwt.token.prefix=Bearer 
jwt.header.string=Authorization
jwt.expirationTime=30000000
#jwt.expirationTime=30000
